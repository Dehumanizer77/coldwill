package i18n

// csMessages is the Czech translation, still to be written. Every key falls
// back to English until it is filled in, so selecting Czech degrades to a
// usable page rather than an empty one.
//
// A runbook is read by someone acting under stress, so this translation should
// be reviewed by a native Czech speaker before anyone prints it.
var csMessages = map[string]string{}
